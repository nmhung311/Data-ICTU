const Footer = () => {
  return (
    <footer className="bg-footer border-t border-border sticky bottom-0 z-40">
      <div className="h-10 sm:h-12 px-4 sm:px-6 flex items-center">
        <p className="text-xs sm:text-sm text-muted-foreground">
          Tải một nguồn lên để bắt đầu – 0 nguồn.
        </p>
      </div>
    </footer>
  );
};

export default Footer;

