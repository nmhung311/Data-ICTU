# API Module

Flask API endpoints cho hệ thống xử lý văn bản.

## Endpoints

- `POST /api/process` - Xử lý văn bản
- `GET /api/health` - Health check
- `GET /api/supported-formats` - Định dạng hỗ trợ

## Cấu hình

- CORS enabled
- File upload support
- Error handling
- Logging
